# Variables públicas disponibles durante `pnpm build:production` y para los
# servicios Next.js. El archivo real vive en /etc/nava/frontend.env y no se
# versiona.
NEXT_PUBLIC_API_URL=https://api.navacloud.app
