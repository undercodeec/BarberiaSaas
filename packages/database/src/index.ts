/**
 * Punto de entrada para los tipos generados de Supabase.
 * El esquema de negocio se agregará mediante migraciones en su fase correspondiente.
 */
export type EmptyDatabaseSchema = Readonly<Record<string, never>>;
